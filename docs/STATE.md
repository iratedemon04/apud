# Apud — STATE
*(Session-survival file. THIS repo copy is authoritative; a fresh session reading this + docs/PLAN.md can continue without re-explaining. Update at end of every session and module close.)*

## Now
- **Three polish fixes 2026-08-01 (same day, user report). 267 tests green, published, smoke-launched, committed + PUSHED.** (1) **Idempotent Ctrl+L**: pushing an already-pushed record that hasn't changed is now a no-op — `PushRecord` returns early when `doc.Stored.Status == Pushed && !doc.Dirty`, so it no longer restamps 005, rewrites the DB row, or rewrites the `<001>.mrk` for nothing (message: "<001> is already pushed and unchanged"). No actual double-dispatch existed; the wasted work was the re-push restamping/rewriting. (2) **Setup wizard trimmed** — `FirstRunForm` cut from a 4-point lecture to two plain sentences + New/Open buttons; smaller form; "Close" not "Done"; status line plain ("Catalogue open. …" / "No catalogue open."). (3) **De-branded / de-LLM'd user text**: removed the org-code EXAMPLE from the Set Organization Code prompt (now "MARC organization code for field 003. Leave blank for none."), plainer org-code result messages, and **genericized every `MX-MxBAC` in the repo to a neutral `XX-XxLib`** (shipped `MrkReader` doc-comment — also "BAC catalogue's existing files" → "other real-world files" — plus the .mrk test fixtures and org_code tests); FieldHelp 003 help reworded (no example, now correctly says Apud fills 003 from the Set Organization Code value). User directive behind (2)+(3): keep the software as general as possible and let no shipped text read as LLM-written.
- **003 org-code auto-fill RE-ADDED 2026-08-01 (same-day follow-up, user changed his mind). File → Set Organization Code…; 267 tests green (+2), published, smoke-launched, committed + PUSHED.** After the Module 10 build removed 003, the user realised he wants it: 003 is a per-catalogue CONSTANT (legitimate to auto-stamp), unlike per-record content. New `catalogue.org-code` command + `MainForm.PromptForText` inline dialog store `org_code` per-catalogue; `PushService` fills 003 from it when set, nothing when blank. NOT a Settings dialog (that stayed cut) — one focused command like Set…Output Folder. See Decisions ("003 org-code auto-fill: RE-ADDED"). This is the current live behaviour; the Module 10 line below still reads as first built (003 removed) — this line supersedes it.
- **Module 10 CODE-COMPLETE 2026-08-01 (pending user acceptance) — the `apud module 10 GO` build. RE-SCOPED MID-BUILD by the user: F1 field help + a first-run/Setup wizard, no Settings dialog. (003 auto-fill was removed here, then RE-ADDED same day — see the line above.)** Mid-build the user corrected the whole Settings premise: *"all org codes are handled by the cataloguer, they catalog with templates so their org code is in the template, no smart behaviour at all, apud doesnt write anything to records except the automatic data and linkeage data."* Two user decisions (via AskUserQuestion): (1) **remove the 003-from-org_code auto-fill** that M9 shipped as "approved exception #3" — it's gone from `PushService.AutoFill` (Apud now writes ONLY 001/005/leader-mechanics + authority linkage; 003 and all other content live in the cataloguer's templates); (2) **no Settings dialog** — everything institution-specific already lives in the portable files (templates, keymap.json, tagnames.json, profile-*.json), and with no record-content defaults there was nothing left for it to hold. So Module 10 shipped: **(A) F1 field help** — `FieldHelp.cs` (config-contract twin of TagNames: built-in one-paragraph help for ~55 common BIB+AUT tags, user-extendable via `taghelp.json` beside the exe — missing→built-ins, bad→reported in the startup line, never crash; uncovered tag → falls back to its TagNames name, never empty), shown in `FieldHelpForm.cs` (a reusable **non-modal** tool-window that re-points to the caret's field/leader each F1 press and never steals editor focus); command `help.field` (F1, **Global** so it works from search or editor), in the Help menu. **(B) Setup wizard** — `FirstRunForm.cs`, a write-nothing orientation dialog (explains: choose a catalogue consciously; record content comes from YOUR templates; Apud fills only 001/005/leader on Ctrl+L; keys are yours via keymap.json; F1 for field help) with New/Open Catalogue buttons wired to the existing actions + a live "you're ready" status; command `help.setup` (Help → Setup Wizard). **Auto-shows once** on a clean install, gated by a new `AppState.FirstRunDone` flag in `%APPDATA%\Apud\ui.json` (SCOPE CALL below), then never on its own again; always reachable from Help → Setup. 265 tests green (+10: FieldHelp config-contract ×8, FirstRunDone round-trip/default ×2), published, smoke-launched. **NOTE:** the smoke launch tripped FirstRunDone→true in the real ui.json; I reset it to false (LastFolder preserved) so the user's first launch still gets the wizard. **NOT committed yet.**
- **Module 10 SCOPE CALL (flagged, user may veto):** the Setup wizard's auto-show-once uses a NEW `FirstRunDone` bool added to `AppState`/`ui.json` — a *second* piece of remembered cross-session state beyond the approved `LastFolder`. It is one-time onboarding (not the "wrong base out of habit" state NO-SMART guards), and Help → Setup keeps the wizard reachable, so if he'd rather nothing auto-appear, delete the `MaybeShowFirstRun()` call (one line) and the flag. F1 help also ships only the **common** tags (not the full MARC21 corpus) — extend via taghelp.json; see DEFERRED.
- **Module 9's five scope calls (A–E) APPROVED by user 2026-08-01 ("all good choices").** Not vetoed — accepted as the intended behaviour. Kept in DEFERRED.md (his request: "good to have them there for future reference"), each now marked APPROVED. They are: (A) fixed-field coded values warn, never block; (B) an unlinked controlled heading doesn't block a push (only rotted links error); (C) country/language lookup table contents still deferred; (D) ripple stays per-record `Update` (single-transaction still technically open, low risk); (E) post-push warnings clear + report a count (Ctrl+W re-shows them with correct jumps). So Module 9 is fully accepted, scope calls and all — clear to start Module 10 on GO.
- **Last-folder memory (2026-08-01, explicit user exception to NO-SMART-BEHAVIOR — "just this once you are allowed smart behaviour", asked to do it + push before sleeping). 255 tests green (+3), published, committed, PUSHED.** New `Apud.App/AppState.cs` = a tiny per-user `%APPDATA%\Apud\ui.json` holding ONLY `LastFolder`; missing/corrupt → blank, load/save never throw (best-effort, no error dialog). `MainForm` gained `_appState`, `StartFolder()` (last folder if it still exists, else `SuggestedFolder()`), `RememberFolder(dir)`. Wired into every File dialog: New / Open catalogue, Import Records, Import Folder (SelectedPath), Export — each opens at the last-used folder and records its folder on OK. Deliberately does NOT remember catalogue/base/window/session — see Decisions. +3 AppState tests (round-trip, missing→blank, corrupt→blank). This is the FIRST persisted cross-session state in the app (the M5b AppSettings store had been deleted by the no-smart-behavior decision).
- **Two more fixes (2026-08-01, same day). 252 tests green (+3), published, smoke-launched. NOT committed.** (1) **New-field placeholder bug**: F6 dropped you into the tag cell but the placeholder ("   " for tag, "__" for indicators) already filled the fixed-width cell to MaxLength, so typing did nothing until you deleted the "bars" first. Fix: the three micro-cells (tag=3, ind=2, code=1) now open with their content **selected** (`ViewerEditingControlShowing`: `micro → tb.SelectAll()`), so the first keystroke replaces the placeholder or an existing code; value/control cells keep caret-at-end. (2) **AUT 001 restart**: he cleared an authority's 001 (a big imported number like `1458764`) in an otherwise-empty base and expected the re-push to give `1`, but got `1458765` — because `MaxControlNumber` counted the record's OWN still-stored 001. Fix (AUT-only, per his explicit "only for the aut base"): `RecordRepository.MaxControlNumber(base, exceptId)` overload excludes the record being pushed; `PushService.AutoFill` passes `rec.Id` for AUT (`ceiling = MaxControlNumber("AUT", rec.Id)`), BIB unchanged (its "001 SPECIALLY DUMB" hand-numbering is deliberately untouched). So clearing an authority's 001 restarts an empty base at 1, else sits one past the highest OTHER authority. Files: `MainForm.cs`, `RecordRepository.cs`, `PushService.cs`, +3 PushService tests. **Note:** the engine already assigned a fresh 001 on AUT delete+push before this — the bug was purely that it counted itself, so the top/only record's number climbed by 1 instead of restarting.
- **Authorities Q&A + import/prune follow-up (2026-08-01, same day, after the UX batch). 249 tests green (+4), published, smoke-launched. NOT committed yet.** He'd made a SEPARATE authorities.db and asked if that's right — clarified the architecture: **ONE catalog.db holds BOTH bases** (`base` column BIB/AUT); they must share one file because authority control is within-file (`heading_link: field_id→auth_record_id`, ripple-on-push, refuse-delete all reference `record.id` inside the same SQLite db — SQLite can't FK across files). A separate authorities.db is unlinkable from bibs. He accepted this: the **per-base output folders** (`MARC_OUT` vs `MARC_OUT_AUT`, added earlier today) already give the on-disk separation he wanted. To fold his separate .db back in: open it → Export Base (AUT) → Import that .mrk into the real catalog.db. **Built this round (a real GO):** (1) **File → Import Records…** (`catalogue.import-file`) — an OpenFileDialog (multiselect .mrk) so the user imports a specific file (or files), not only a whole folder; routes to BIB/AUT by leader like folder import; the old Import Folder stays (`catalogue.import-folder`, renamed menu label). Both now share `RunImport(source, plan)`; `ImportEngine.Analyze(IEnumerable<string>)` already existed. So the **MarcEdit-convert-then-import authority path works today** (Path A). (2) **Delete Selected Fields** (`field.delete-selected`, default **Ctrl+Shift+F5**, rebindable) — grid is now `MultiSelect=true`; select a range of fields (Shift/Ctrl-click or Shift+arrows) and delete them in ONE undoable step (`EditorDocument.DeleteFields`). This is Path B's missing piece (prune a pasted-in record down to a new AUT record). Files: `MainForm.cs`, `EditorDocument.cs`, `ImportWizardForm.cs` (title "Import Records"), `keymap.json`, tests (+DeleteFields, +single-file AUT-routing import). **DEFERRED / not asked-for:** PLAN §7 "plain-text authority list import" (heading-per-line) still future; cross-file ATTACH of an external authority db explicitly decided AGAINST (single-file design stands).
- **Editor UX batch (2026-08-01, user's numbered task list — a "NO GO, first some tasks" round, NOT a module). 245 tests green (+7), published, smoke-launched.** Delivered: (1/8) the record grid now behaves like a text editor — **Insert drops into the current cell with a caret at the end** (new `field.edit` command, default **Insert**; `BeginEdit(false)`), and **every** cell edit opens with a caret, never select-all (`ViewerEditingControlShowing` places the caret at text end via a deferred `BeginInvoke`). (5) **Keyboard flow through a new field**: typing a tag / creating the first subfield used to re-render and fling the cursor to the top (forcing a click); `RenderRecord(preservePosition)` now captures the cursor by MODEL index (field/subfield/column) and restores it after the rebuild, and a structural edit sets `_resumeEditAfterRender` so the caret drops back in — so tag → Tab → indicators → Tab → code → Tab → value stays entirely on the keyboard. `NewField`/`NewSubfield` begin edit immediately. (2a) **Ctrl+F5 delete now focuses the grid** so the cursor visibly lands on the field below (`SelectCell` calls `_viewer.Focus()`; Ctrl+F7 already felt right because of focus). (6) **Search: Down from the box steps into the results list** (selects first hit), **Up from the first hit returns to the box** — pick a hit without the mouse. (2b) **Copy/paste field & subfield**: `Ctrl+T` copy field / `Alt+T` paste field, `Ctrl+S` copy subfield / `Alt+S` paste subfield (deep clones on a MainForm clipboard that survives switching records; `EditorDocument.CopyField/PasteFieldAfter/CopySubfield/PasteSubfieldAfter`, all undoable, control-field paste refused). **These reassign two defaults (user chose "take the keys, move Save"): Save Draft → `Ctrl+D`, Save as Template → `Ctrl+Shift+T`** (both still in the Record menu; all rebindable in keymap.json). (9) **Authority records now mirror to their OWN user-chosen folder** — File → **Set Authority Output Folder…** (`catalogue.marc-out-aut`, setting `marc_out_folder_aut`, default `MARC_OUT_AUT` beside the .db); the old "Set MARC Output Folder" is now **Set BIB Output Folder** (`marc_out_folder`, `MARC_OUT`). Separate folders because BIB/AUT number 001s independently (BIB 758.mrk vs AUT 758.mrk would collide). Push/Delete are base-aware (`MarcOutFolder(base)`). (10) **Field tags are gray, not black** (tag column ForeColor = Gray). All in `Apud.App/MainForm.cs`, `EditorDocument.cs`, `RecordMirror.cs`, `keymap.json`. NOT committed/pushed yet (awaiting his test-drive).
- **Module 9 CODE-COMPLETE 2026-07-31 (pending user acceptance) — the `apud module 9, GO` build in one turn. Ctrl+L (validate + push) and Ctrl+W (validate only) are live.** Ctrl+W runs the whole pipeline as a dry run (writes nothing); Ctrl+L pushes a clean record and blocks a dirty one. A **findings list** now sits below the record (hidden until there are findings); each row is colour-coded Error/Warning and **click or Enter jumps the cursor to the offending field** (§5 — the message bar was only a label before). Five stages (docs/PLAN.md §8): (1) structural — bad tag, bad indicator, bad subfield code (catches the `$Preciado` dropped-delimiter → uppercase-code slip), empty subfield, empty control field, data field w/ no subfields; (2) fixed-field — LDR/008 length (error) + coded `values` positions vs the **same M7 `FixedFields` layouts** (mismatch = WARNING, blank/`|` fill skipped); (3) profile — mandatory / non-repeatable / required-subfield / single-1XX rules from a **user-editable `profile-bib.json`/`profile-aut.json`** (keymap/tagnames contract: missing→built-in default, bad→report+default); (4) authority — a stored link that ROTTED errors (`auth.missing` deleted / `auth.drift` text changed), an UNLINKED heading does NOT block; (4b) duplicate hand-typed 001 = error. Auto-fill (only after 0 errors): **001 = live `MAX(001 in base)+1` computed at push when empty, hand-typed 001 kept forever** (Decisions "001 SPECIALLY DUMB" — NO stored counter; `sequence` table now vestigial), **005** stamp, **003** from `org_code` setting only if set (none yet → skipped), **stable field sort by tag** (repeated 650/500 order preserved), **LDR 00-04 length + 12-16 base address recomputed** (ISO 2709 UTF-8 byte math, `Marc.Core/LeaderMechanics.cs`) + constants 10/11/20-23. AUT push then **ripples** the new 1XX into linked bibs (reuses M8 `RewriteLinkedBibHeadings`, reports the count). 232 tests green (+34), published, smoke-launched; profile-*.json ship in publish\. Acceptance step: open a draft, Ctrl+W to see the findings list, click one to jump; fix it; Ctrl+L → it pushes with a fresh 001 and reorders; rename an AUT heading + Ctrl+L → linked bibs ripple.
- **Module 9 SCOPE CALLS (flagged, user may veto — all in DEFERRED.md):** (A) fixed-field coded-value mismatches are **warnings, never errors** (a byte Apud doesn't recognise never blocks a push — faithful/dumb ethos + his bracket/fill data). (B) an **unlinked** controlled heading does NOT block push — only rotted links error (literal §8.4 "every controlled field must be linked" would make his whole link-less imported catalogue un-pushable, unlike Aleph). (C) **country/language lookup TABLE CONTENTS deferred again** (framework ships; warning-only, held back as noise-risk). (D) ripple stays **per-record Update** — strict single-transaction guarantee still the open DEFERRED item. (E) on a successful push the panel clears + message-bar reports the warning COUNT (post-reorder field-refs would mis-jump); Ctrl+W re-shows warnings with correct jumps.
- **Module 8 ACCEPTED by user 2026-07-31 ("everything ok").** AUT base authority control (browse index + Ctrl+F4 linking), the whole `apud module 8, GO` build in one turn. On a BIB record, stand on a controlled heading field (100/6XX/7XX/8XX etc.), **Ctrl+F4** → a modal browse list of the AUT base opens **positioned alphabetically at that field's text** (accent/case-insensitive), authorized headings interleaved with their see-refs (`variant → see: authorized form`); Enter/double-click on a heading **rewrites the field to the authorized form and stores the link** — then **Ctrl+Z reverts it** (link write-back rides the undo stack). 198 tests green (+40), published, smoke-launched. His already-imported catalog.db lights up on first open: a v2→v3 migration builds heading_index from every pushed AUT record — no re-import. **The link is a per-field pointer (`heading_link: field_id → auth_record_id`, `MarcField.AuthLinkId`); forged at Ctrl+F4 (or, later, import exact-match), stored on save; validation (M9) VERIFIES it, never creates it.**
- **Module 8 SCOPE CALLS (flagged, user may veto):** (1) **Ripple + refuse-delete are built and unit-tested as engines** (`RewriteLinkedBibHeadings`, `Delete` guard) but their LIVE trigger is the Ctrl+L push cycle = Module 9; refuse-delete is wired into the reachable `repo.Delete` now, ripple fires when M9 pushes an AUT record. (2) **Browse indexes PUSHED AUT only** (matches FTS) — draft AUT headings become browseable when pushed (M9); his data imports as pushed so nothing's missing. (3) **Ctrl+F4 is BIB→AUT linking only**; AUT-side browse deferred. (4) **Heading display = subfield values space-joined**, no invented MARC display punctuation (dumb-software ethos); the normalized key, not the display, drives matching. Deferred items live in DEFERRED.md.
- **Post-Module-7 fix (2026-07-31, user report): displayed Year now comes ONLY from 260/264 $c, never the 008.** His real data stuffs brackets/fill into the fixed 4-char 008 date slot — record 177's 008 held `[1960]` (showed as the truncated `[196`); 32 records hold `####`. `YearOf` no longer reads the 008 (the 008 subquery was dropped from `List`); the transcribed $c is the truth on screen. Record-view 008 row still shows the literal bytes (user: "since it's literal it's ok"). Verified on catalog.db: rec 177 → `[1960]`, the `####` records → blank. 158 tests green (one year test rewritten, Monograph fixture gained a 264). See Decisions.
- **Module 7 ACCEPTED by user 2026-07-31 ("everything's ok"), after the Year fix above and the layouts/templates Q&A. Module 8 is the live work — see handoff at the bottom. Ctrl+F3 on the LEADER or an 008 opens a modal position-by-position editor (two columns of labeled byte boxes with live code meanings, protected/computed positions greyed, 008/00-05 pre-fills today). Writes back through EditorDocument so Ctrl+Z covers it. Engine (`Marc.Core/FixedFields/`) is data-driven: byte maps are embedded JSON, the SAME objects the Module 9 validator will reuse. Ships LDR(bib/auth) + 008 for all 8 materials (BK/CR/MP/MU/VM/CF/MX/authority); material picked from LDR/06-07 per MARC21. 158 tests green (+21), published, smoke-launched. Acceptance step: open a record, stand on 008, Ctrl+F3, set Type of date + a code, OK, watch the 008 row change; then Ctrl+Z reverts it. On "move on" → Module 8 (handoff below).**
- **Module 7 SCOPE CALLS (flagged, user may veto): 006 + 007 layouts DEFERRED (DEFERRED.md) — engine is generic, both are zero-code JSON additions later; a book catalogue rarely hand-edits them. Country/language lookup TABLE CONTENTS deferred to Module 9 (the validator consumes them); the `lookup` attribute already ships in the schema and the dialog shows "(country/language code)" hints.**
- **Module 6 CODE-COMPLETE (steps 1-9 + 2 post-review tweaks), 2026-07-31 — user moved on to Module 7 without red-penning (implicit accept). Acceptance step: edit a record, save draft, new-from-template, and REBIND A KEY in keymap.json + restart. 128 tests green, published, smoke-launched.**
- **Post-review tweaks (2026-07-31, user asked after the step-9 summary):** (1) tag cell capped to 3 chars (indicators 2, subfield code 1) via DataGridView EditingControlShowing → TextBox.MaxLength — any characters allowed, only the width is fixed (still dumb). (2) Full undo/redo: `EditorDocument` routes every mutation through `Apply()`, which snapshots the whole record (cheap — records are tiny) and keeps the snapshot only if something changed; `Undo()`/`Redo()` restore. Dirty is now signature-based (differs-from-saved), so undo-to-saved clears the `*`. Bound Ctrl+Z / Ctrl+Y (Editor context, rebindable). Undo handler EndEdits first so it also reverts the cell you're typing in. +9 tests.
- **Steps 1-2 (2026-07-31):** command table (`Commands.cs` — Command id/name/context/DefaultKey/Execute; CommandRegistry; menus render via MainForm.MenuItem(id), text/handler/shortcut-display from one entry) + keymap engine (`Keymap.cs` — keymap.json beside exe, tagnames.json contract: missing→default, bad entry/conflict→reported in message bar+default kept/first wins, ""=unbind, comments ok; context-scoped Global/Search/Editor, Search+Editor may share a chord; dispatch = MainForm.ProcessCmdKey standard WinForms hook + typing guard incl. IsCurrentCellInEditMode; chord parse = Keys enum names + aliases Esc/Del/PgUp). User framed 1-2 as plumbing, not a headline feature.
- **Steps 3-9 (2026-07-31):** editor grid now editable in place (`EditorDocument.cs` = headless per-record edit ops, all tested; MainForm hosts it via _currentDoc + DataGridView CellEndEdit). Dumbness honored: any 3-char tag typed freely, no entry validation, fields NEVER reordered (that's Ctrl+L, M9); control↔data retag only on an empty field. F6 blank field / F7 subfield / Ctrl+F5 del field / Ctrl+F7 del subfield; `*` dirty marker in header. Ctrl+N context-sensitive (no record→template picker; in record→copy-as-draft, 001 cleared). Ctrl+S = SaveDraft (RecordRepository.SaveDraft demotes pushed→draft, out of search). Ctrl+T save-as-template (MrkWriter). Ships templates\ (monograph/serial/authority .mrk, 40-char 008, no 001). Stubs wired w/ real rebindable keys: Ctrl+F3→M7, Ctrl+F4→M8, Ctrl+W/Ctrl+L→M9 (message-bar only). DisplayRow gained FieldIndex/SubfieldIndex back-refs for the grid. +47 tests total this module.
- **User working rule (2026-07-31, applies everywhere): don't reinvent the wheel — solved problems get the platform's standard mechanism (ProcessCmdKey, Keys enum, System.Text.Json), thinnest wrapper on top; "work like a senior dev."**
- **§6.2 keymap red-pen received (in PLAN.md §6.2):** Ctrl+F3 on LDR/008 = position-by-position editor (Module 7); Ctrl+N context-sensitive (no record → new; in a record → copy as draft, 001 cleared); Ctrl+L = validate+push (ASSUMED from "ctr for pushing" — confirm with him); Ctrl+F4 = validate field / on heading: AUT popup positioned alphabetically, Enter links both records (Modules 8–9). Defaults don't matter much — **the hard requirement is user-rebindable hotkeys (keymap.json). That's the feature.**
- **Module 6 step list (presented 2026-07-28, amendments folded in):**
  1. Command table: every editor action a command object (id/name/context/handler); menus render FROM the table from now on.
  2. Keymap engine (the centerpiece): keymap.json next to exe, `"command-id": "Ctrl+F3"`, comment header documenting syntax; missing file/entry → built-in default; bad binding → report line, fall back, never crash; context-scoped (editor/search/global); headless tests.
  3. Editor grid: Record view becomes editable in place (subfield values, codes, indicators, tags); **new field = BLANK field, cataloguer types ANY tag (000/999/whatever) — no pick-list, no entry-time validation (dumb software)**; F6 new field, F7 new subfield, Ctrl+F5/Ctrl+F7 delete; **editor NEVER reorders fields — ordering happens at validation (Module 9) as a STABLE sort by tag: repeated tags (650s, 500s, 900s) keep the cataloguer's written order (subject order is real information)**; dirty marker (*) in header bar.
  4. Ctrl+N context behavior (new-from-template picker / copy-as-draft with 001 cleared, sequence fills at push).
  5. Ctrl+S save draft via existing repo (drafts stay out of search per 5a).
  6. Templates: .mrk skeletons in templates\ beside exe; Ctrl+T save-as-template (user names file); ship 3 starters (monograph, serial, authority).
  7. Stubs wired not built: Ctrl+F3 → "arrives in Module 7", Ctrl+F4 → M8, Ctrl+L → M9 (message bar); keys rebindable from day one.
  8. Tests: keymap parse/fallback/conflicts, dispatch, editor ops on the model, Ctrl+N copy semantics, template round-trip.
  9. Publish + his test drive (edit, save draft, new-from-template, REBIND A KEY and feel it work) → STATE, commit, push.
- **Pre-Module-6 tweaks (2026-07-29, user request):** (1) tag names are user-editable via `tagnames.json` beside the exe (same contract keymap.json will follow: comment header in the file, missing file/entry → built-in default, bad entry skipped + reported in message bar, broken file reported + built-ins stand, never crashes; ships with all defaults listed; JSON comments/trailing commas tolerated). (2) Search results / List All now show Author (1XX first subfield, else first 700/710/711) and Year (008 Date 1, else 260/264 $c as written) columns; RecordSummary carries both. +8 tests (72 green), published.
- Module 5c ACCEPTED by user 2026-07-28 ("this looks great") after the layout rework.
- **5c layout REWORKED after user correction (2026-07-28):** search screen (base+scope+query form, results, history) lives in the MAIN area with a Search/Record view toggle; the SIDEBAR is the open-records collection — double-click/Enter on a result adds the record (any base, no duplicates, Remove/Remove All/Del to close); List All button = explicit whole-base listing; nav-list-replacement design scrapped. ALEPH-WORKFLOW.md search section corrected.
- Module 5b ACCEPTED by user 2026-07-28 ("works beautifully") after the no-smart-behavior fix.
- 2026-07-28: user supplied 5 screenshots of his real ALEPH v24 workflow → distilled into `docs/ALEPH-WORKFLOW.md`. UI modules 5b–8 must follow that reference (subfield-per-row layout, header bar, menu-based base switch, search history grid, F8 dialog shape).
- **Module 5b:** catalog file handling (default `Documents\Apud\catalog.db`, New/Open; ~~remember last path~~ CUT by no-smart-behavior decision), navigation pane (BIB/AUT switch, record list: 001/title/status), read-only viewer with ‡a subfield display (NO dollar signs on screen), plain File→Import Folder calling the 5a engine. Acceptance: he imports his catalog himself and browses it.
- **Module 5c (after 5b):** search box wired to FTS, full import wizard (report grid, pushed/drafts choice, cancel = nothing committed), Export UI. Acceptance: he searches in Spanish and re-imports via wizard.
- Environment note: .NET 8 SDK 8.0.423 installed via winget (machine had runtime only). Git identity: repo-local iratedemon04 <iratedemon04@gmail.com>; history rewritten 2026-07-27 to purge old-account attribution (ranakamikaze), force-pushed by user; contributors list verified clean via API (graph cache may lag).

## Module sequence (user gates every transition — build ONLY current module)
1. Scaffold ✔ · 2. MARC model + .mrk ✔ · 3. ~~.mrc~~ CUT · 4. Database ✔ · 5a. Import/Export/FTS engines ✔ · 5b. Catalog on screen ✔ · 5c. Search + wizard + export ✔ · 6. Editor+keymap+templates ✔ · 7. Ctrl+F3 fixed-field dialogs ✔ · 8. AUT+Ctrl+F4 authority control ✔ · **9. Ctrl+L validate+push pipeline ✔ CODE-COMPLETE (pending accept — findings list, 5 stages, auto-fill 001/005/LDR, ripple-on-push)** · **10. F1 help + first-run/Setup wizard ✔ CODE-COMPLETE (pending accept — Settings dialog CUT; 003 auto-fill via File→Set Organization Code)** · **11. Sync (SFTP) ← NEXT** · 12. Distribution (installer, manual, release tag)

## Done
- **Module 10 — F1 field help + first-run/Setup wizard** (2026-08-01, pending user acceptance; Settings dialog CUT and 003 auto-fill REMOVED, both by user). **`Apud.App/FieldHelp.cs`** — offline MARC21 help, one paragraph per ~55 common BIB+AUT tags, config-contract twin of TagNames (`taghelp.json` beside exe overrides/extends; missing→built-ins, non-object/bad-entry→reported + built-ins stand, JSON comments/trailing commas tolerated, never crash; `For(tag)` falls back to the TagNames name, then a generic line — never empty; 9XX → "reserved for local use"). **`FieldHelpForm.cs`** — reusable non-modal `SizableToolWindow`; `ShowHelp(tag)` sets header (`tag — name`) + body, F1 re-points it, editor keeps focus. **`FirstRunForm.cs`** — write-nothing Setup wizard (workflow orientation + New/Open Catalogue buttons + live ready-status). **`AppState.FirstRunDone`** — new bool in `ui.json`, gates the once-only auto-show. **MainForm**: commands `help.field` (F1, Global) + `help.setup`; Help menu gains both above About; `FieldHelp.LoadFile` joins the startup config-report aggregation; `MaybeShowFirstRun()` on Load shows the wizard once then saves the flag; `ShowFieldHelp()` resolves the caret's tag via `CurrentRef()` (leader→"LDR"), else a hint. Ships `taghelp.json` as Content. **`Apud.Data/PushService.AutoFill`**: 003-from-`org_code` was deleted here, then RE-ADDED the same day with a **File → Set Organization Code…** command (`catalogue.org-code` + `MainForm.PromptForText`) — see the "003 org-code auto-fill: RE-ADDED" decision; net state = 003 fills from a per-catalogue org_code when set. +12 tests (267 green: the 10 below plus 003-filled-when-set / none-when-unset): FieldHelp override/fallback/uncovered-never-empty/9XX-local/comments/broken-reported/non-text-skipped/missing-ok/shipped-file-clean, FirstRunDone round-trip + defaults-false. Published + smoke-launched. **DEFERRED: full MARC21 tag corpus for F1 (common tags shipped; extend via taghelp.json).**
- **Module 9 — Ctrl+L validate + push pipeline** (2026-07-31, pending user acceptance). **`Marc.Core/Validation/`** (pure, headless — the whole error corpus is tested without a DB or WinForms): `ValidationFinding(Severity, FieldRef?, Code, Message)` + `FieldRef` (editor index convention: -1=leader, subfield -1=whole field — what lets the findings list jump the cursor); `ValidationProfile` (Mandatory groups / NonRepeatable / RequiredSubfields / SingleHeading1xx + lean per-base `Default`); `RecordValidator.Validate(record, base, profile)` runs stages 1-3 (structural, fixed-field, profile). **`Marc.Core/LeaderMechanics.cs`** — `Recompute` fills LDR 00-04 length + 12-16 base address (ISO 2709 layout, **UTF-8 byte** counts) and constants 10='2'/11='2'/20-23='4500'. **`Apud.Data/PushService.cs`** — `Check` (Ctrl+W, no write) = RecordValidator + stage 4 authority (loads each linked auth, `auth.missing`/`auth.drift` on rot; unlinked = silent) + stage 4b dup-001; `Push` (Ctrl+L) blocks on any error else auto-fills (001 live-max+1-if-empty / 005 / 003-if-set / stable tag sort / leader recompute), sets Status=Pushed, Insert-or-Update, and for AUT calls `RewriteLinkedBibHeadings` → `PushResult(Ok, Findings, ControlNumber, RippledFields)`. **RecordRepository** gained `MaxControlNumber(base)` (live MAX, non-numeric→0) + `ControlNumberUsedElsewhere` (dup check). **`Apud.App/ValidationProfileConfig.cs`** — loads `profile-<base>.json` beside exe (keymap/tagnames contract), `For(base)`→loaded-or-default; ships documented `profile-bib.json`/`profile-aut.json` as Content. **MainForm**: `_findings` ListView docked below the record (Error red / Warning goldenrod, DoubleClick + Enter → `JumpToSelectedFinding` moves the grid cursor), `ValidateRecord`/`PushRecord` wired to the real Ctrl+W/Ctrl+L commands (were message-bar stubs), profile files loaded into the startup config-report line. +34 tests (232 green): the seeded corpus (bad tag, uppercase code, empty subfield, no-subfield field, bad indicator, short 008, missing/repeated 245, dup 001, single-1XX, missing-1XX), fixed-code warning-not-error + fill-skipped, clean push earns 001 / stamps 005 / recomputes leader / orders fields keeping repeated-650 order, live max+1 (757→758) / hand-typed 001 kept / dup blocks, error blocks+writes-nothing, Ctrl+W dry-run, auth.missing/auth.drift block + unlinked doesn't, ripple count + text, leader byte math (ASCII + UTF-8), profile config valid/broken/malformed. Published + smoke-launched. **DEFERRED (scope calls A-E): fixed-field warnings-only, unlinked-not-blocked, country/language table contents, ripple single-transaction, post-push warning jumps.**
- **Module 8 — AUT base authority control** (2026-07-31, pending user acceptance): the NACO-style normalization + browse index + Ctrl+F4 link cycle. **`Marc.Core/HeadingNormalization.cs`** — the ONE `Normalize()` (NFD decompose → strip combining marks → casefold → keep letters/digits + the FIRST comma, every other punctuation→space → collapse) reused by index + browse (+ M9 validator); exhaustively tested on Spanish data (ñ→n, ¿¡ stripped, accents folded, first-comma kept, `Física`≡`fisica`). **`Marc.Core/Headings.cs`** — `Extract(authRecord)` → 1XX(auth)/4XX(see)/5XX(seealso) entries (kind/tag/display/normalized); `HeadingText(field)` = subfield values space-joined minus relators ($e/$4); `ApplyAuthorizedHeading(bibField, authRecord)` copies the auth 1XX subfields + Ind1 into the bib field, **preserving the cataloguer's $e/$4 relators (appended at the end)** and the field's Ind2; `IsControlledBibTag` (100/110/111/130/240/6XX/7XX/8XX). **`Apud.Data/HeadingIndexer.cs`** (parallel to FtsIndexer) — maintains heading_index over PUSHED AUT records; hooked into RecordRepository Insert/Update/Delete; `Rebuild` for the migration. **RecordRepository** gained `BrowseHeadings(normalizedStart)→BrowseResult(entries, position)` (a positioned window: context above + entries at/after the point, normalized order), `LinkedBibIds`, `RewriteLinkedBibHeadings` (ripple engine, returns fields-rewritten count), and a **refuse-delete guard** on `Delete` (throws if the AUT record still has links; heading_index cascades, heading_link deliberately does not). **Schema v3**: `UpgradeV2ToV3` rebuilds heading_index from pushed AUT records so pre-Module-8 catalogues gain the index with no re-import. **`Apud.App/AuthorityBrowseForm.cs`** — modal Ctrl+F4 popup: position box (type+Enter re-positions), interleaved list (see-refs greyed, `variant → see: authorized`), Enter/double-click/Link picks; `EditorDocument.LinkAuthority` routes the write-back through `Apply()` so **undo covers heading text + link together**; wired `field.validate` (Ctrl+F4). +40 tests (198 green): normalization corpus, extraction, write-back preserving $e, index build (pushed only), positioned/accent-insensitive browse ordering, ripple count + relator preservation, refuse-delete, migration rebuild. Published + smoke-launched. **DEFERRED: 006/007 layouts (M7), lookup-table contents (M9), AUT-side Ctrl+F4 browse, MARC display punctuation.**
- **Module 7 — Ctrl+F3 fixed-field position editor** (2026-07-31, pending user acceptance): data-driven layout engine in `Marc.Core/FixedFields/` (NO UI/DB, so Module 9's validator reuses it as the single source of truth) — `FixedFieldPosition` (off/len/name/auto/protected/lookup/values), `FixedFieldLayout` (field/material/length/positions, key "008/BK"), `FixedFieldData` (reads a control string against a layout, always exactly layout length: pads short/truncates long, blanks=spaces, `Slice`/`Set`/`ToString`), `FixedFieldLayouts` (loads embedded JSON once via GetManifestResourceStream; `Material008(leader)` = MARC21 config-of-008 from LDR/06-07 covering every 06 value; `Leader`/`For008`/`Get`). Byte maps are **embedded JSON** (`FixedFields/layouts/*.json`, `<EmbeddedResource>` in Marc.Core.csproj) not code — adding a material later = one JSON file. Shipped: LDR bib + LDR authority; 008 for BK, CR, MP, MU, VM, CF, MX, authority (all 40-char, contiguous). `FixedFieldForm` (WinForms, the ONE place boxes-you-fill-out are right per Decisions): modal OK/Cancel, two mirrored columns of `label | box | meaning`, MaxLength=position width, protected positions greyed & TabStop off, `values` decode shown live beside the box ("— undefined code —" for unknowns), `lookup` positions hint "(country/language code)", 008/00-05 pre-fills today (YYMMDD) when blank; all logic in `FixedFieldData` so it's tested without WinForms. Wired `field.fixed-edit` (Ctrl+F3, already a rebindable command): resolves target from the caret — leader row → LDR layout, 008 field → material layout; other control fields → "type its value directly" message; data field → "stand on the leader or 008"; write-back routes through EditorDocument.SetLeader/SetControlData → **undo covers it**, then RenderRecord + sidebar refresh. +21 tests (158 green): layout contiguity invariant (every byte covered, no gaps/overlaps, right length), Material008 from leader (13 cases), leader bib-vs-authority, 008 byte round-trip, **M4 build-008-from-positions byte-for-byte vs hand-written reference**, Set pad/truncate, value/lookup attribute checks. Published + smoke-launched. **DEFERRED (scope call): 006/007 layouts (zero-code JSON additions later) + country/language lookup table CONTENTS (→ Module 9 validator).**
- **Module 5.9 — Aleph editor look** (2026-07-28): viewer restyled per user ("free editor, not boxes"): CellBorderStyle None, padding 0, tight rows; tag/code columns red bold underlined (keep color under selection), field names small maroon italic, values bold Consolas; docs/inspiration/README.md = written spec of his 5 Aleph screenshots (he may drop PNGs there). Republish was blocked once by his running Apud.exe — republish after he closes it.
- **Module 5c — Search + import wizard + export UI** (2026-07-28, pending user acceptance): SearchScope enum (All/Title/Author/Subjects/ControlNumber) → FTS5 column filters in BuildMatchExpression (`title : ("x"*)`; hostile input safe in every scope); search bar over nav pane (scope dropdown + box; Enter or Search; results replace nav list in rank order; Clear → full list; drafts invisible by construction); session SearchHistory (newest first, repeats kept, IN-MEMORY ONLY per no-smart rule; grid bottom-left: query/scope/hits; double-click re-runs incl. base switch); ImportWizardForm replaces 5b MessageBox flow (per-file grid w/ warning/error counts + color, click→line-numbered diagnostics, run errors in red, PUSHED/DRAFTS radios w/ rules stated, Import enabled only when chosen mode legal, defaults to strictest legal, Cancel commits nothing; commit-time SqliteException → rolled-back + friendly dialog); Export UI: File→Export Base... / Export Selected... (nav is multi-select now; SaveFileDialog, user names path, suggested `BIB.mrk`/`BIB-selection.mrk`, overwrite prompt). Cross-sets deferred (DEFERRED.md). +4 tests (64 green). Published + smoke-launched.
- **Module 5b — Catalog on screen** (2026-07-28, ACCEPTED by user): catalog lifecycle (File→New/Open; starts disconnected every session; Open refuses non-existent paths; suggests `Documents\Apud` only if the user made it; newer-schema error → friendly dialog, title bar shows path; NO remembered state per the no-smart-behavior decision); Base menu BIB/AUT with radio check (Aleph Connect-to style); nav pane ListView (001/Title/Status, control-number order, count in message bar); RecordDisplay view model (headless, tested; Module 6 editor reuses it) — subfield-per-row, `^` blanks in LDR/control data, `_` blank indicators, literal `$` shown as-is, never .mrk notation; TagNames English table; DarkRed header bar (base + 001 + 245/1XX heading); read-only DataGridView viewer (name/tag/ind/code/value, styled columns); plain File→Import Folder → Analyze → any error aborts with line-numbered text, else PUSHED/DRAFTS choice (Yes/No/Cancel) → commit → nav refresh. Tests +9 (62 green; Apud.Tests retargeted net8.0-windows to reference Apud.App). Published + smoke-launched OK.
- **Module 5a — Import/Export/FTS engines (headless)** (2026-07-28): Schema v2 — `record_fts` recreated with `tokenize='unicode61 remove_diacritics 2'` (accent-insensitive; migration chain v1→v2 drops/recreates and reindexes via FtsIndexer.Rebuild). FtsIndexer (internal): contentless-FTS maintenance — Add/Remove/Rebuild build column values from DB field rows (title=245, author=1XX/7XX, subjects=6XX, anytext=all data fields, control_number); PUSHED records only; Insert/Update/Delete in RecordRepository keep the index in step (Remove runs BEFORE field rewrite/cascade so delete-rows carry the exact indexed values). `Search(base, query, limit)` — ranked ids; user text → quoted prefix terms, hostile input can't cause FTS syntax errors. ImportEngine: Analyze(files|folder) → per-file report (record counts, line-numbered diagnostics) + run errors (duplicate 001s within run and vs catalogue, per base); records routed BIB/AUT by LDR/06; Commit = ONE transaction, all-or-nothing, then BumpSequencePast(highest numeric 001) per base. **Semantics decided:** AS-PUSHED blocked by ANY error; AS-DRAFTS tolerates parse errors (its purpose = bring imperfect data in for fixing) but duplicate 001s block both modes (DB constraint); drafts keep their 001s. ExportEngine: whole base (control-number order, drafts included — export is backup, not publication) or id-selection → MrkWriter.ToBytes. 18 new tests (53 total green) incl. v1→v2 migration test. **Dry-run vs 654 live files (read-only): 654/654 records, 0 errors/warnings/duplicates; analyze 176 ms, commit 339 ms; search "fisica"="física"=4 hits; export 428 KB re-parses clean; 654/654 byte-identical round-trip; next BIB 001 after bump = 758.**
- **Module 4 — Database** (2026-07-27): Microsoft.Data.Sqlite 8.0.11; ApudDatabase (open/create/in-memory, FK+WAL pragmas, user_version migrations, full v1 schema incl. heading_link/heading_index/record_fts created up front); StoredRecord/RecordSummary/RecordStatus; RecordRepository — Insert/Update (fields+links rewritten in ONE transaction; AuthLinkId round-trips), Load, List (245/1XX title, control-number order), Delete (cascades), CountLinksTo, NextControlNumber + BumpSequencePast (per-base), settings. Partial unique index on (base, control_number) WHERE NOT NULL → drafts without 001 coexist; duplicate 001 throws. Subfields packed with U+001F. 11 new tests; DB round-trip proved via .mrk byte equality. Module 2 dialect facts: HIS files are the spec; {dollar} literal-$ kept (LC-confirmed).
- **Module 2 — MARC model + .mrk** (2026-07-27): Marc.Core model (MarcRecord/MarcField/MarcSubfield, RecordKind from LDR/06, AuthLinkId slot on fields, order-preserving); Mrk namespace: MrkReader (permissive, line-numbered recoverable diagnostics — structural sins preserved as data for the Module-9 validator), MrkWriter (canonical UTF-8 no-BOM/LF via ToBytes), MrkDiagnostic/MrkReadResult. 23 tests: round-trips (diacritics, 008 trailing spaces, repeated fields, multi-record, CRLF, BOM, {dollar}), diagnostics (junk lines, $Preciado preserved+roundtripped, missing/short leader, missing separator, empty subfields, bad tags). Full live-catalog sweep: 654/654 clean.
- **Module 1 — Scaffold** (2026-07-27): repo `C:\Users\ACV\Projects\Apud\` (git, main branch); Apud.sln with Marc.Core / Apud.Data / Apud.Sync / Apud.App / Apud.Tests wired per dependency rule; MainForm shell (menu stub, message bar); MarcConstants + 1 wiring test green; publish.ps1 → self-contained `publish\Apud\Apud.exe` (AssemblyName=Apud, v0.1.0), smoke-launched OK; docs/PLAN.md, docs/STATE.md, docs/DEFERRED.md.
- Full technical plan: `docs/PLAN.md` (architecture, schema, keymap proposal, validation pipeline, milestones). Self-validated once; known verify-item: MarcEdit literal-$ convention (check in Module 2).

- **003 org-code auto-fill: RE-ADDED same day (user, 2026-08-01) via File → Set Organization Code — the ONE org-level value Apud fills; no other record content.** The user first said remove all 003 ("org code is in the template"), then the same day realised he does want the auto 003 and asked to add it back ("is it too complicated"). Reconciled cleanly: **003 is not per-record content, it is a per-catalogue CONSTANT**, so it is legitimate for Apud to stamp it — unlike language / classification / other content, which stay in the cataloguer's templates. Mechanism (NOT a Settings dialog — that stayed cut): a single focused **File → Set Organization Code…** command (`catalogue.org-code`) mirroring the Set…Output Folder commands, storing `org_code` per-catalogue in the `setting` table via a tiny inline text prompt (`MainForm.PromptForText`). `PushService.AutoFill` fills 003 from it when set, writes nothing when blank/unset (blank = auto-fill off). So Apud writes: 001 (live max+1 when empty, hand-typed kept) + 005 + stable tag order + LDR length/base-address (all mechanical) + **003 (the org constant, when set)** + authority linkage (links + ripple). Everything else is the cataloguer's, from templates. The two LDR/008 auto-write exceptions are unchanged (Ctrl+F3 008/00-05 today-fill on OK-when-blank; template skeletons). +2 PushService tests (003 filled when set; none when unset/blank).
- **MARC output mirror + Delete Record (user request 2026-07-31, post-M9 accept "everything's ok"; folder made CONFIGURABLE on 2nd request):** every successful **Ctrl+L push mirrors the record to `<output folder>\<001>.mrk`** (`Apud.App/RecordMirror.cs`, MrkWriter bytes; folder made on demand; overwrites on re-push; write failure reported but never undoes the committed push). **The output folder is user-chosen: File → Set MARC Output Folder… (`catalogue.marc-out`), stored per catalogue in the `setting` table (key `marc_out_folder`).** When unset it defaults to a `MARC_OUT` subfolder beside the .db. **The push message now names the exact file+folder written** (or says none is set) — the first "beside-the-db" version silently wrote to wherever catalog.db actually was, which is why he didn't find `758.mrk` (his .db is presumably not in `…\Record & search\`); the message + explicit folder fix that. New **Delete Record** command (`record.delete`, Editor context, default **Ctrl+Delete**, rebindable; Record menu) removes the DB row AND the .mrk file (in the current output folder), after a Yes/No confirm (irreversible). **Deliberate user-approved exception to NO-SMART-BEHAVIOR "creates nothing on its own"** — he weighed it and asked for it. Safe by design: DB stays source of truth (one-way export); mirror touches ONLY the output folder, never his read-only source; a linked-AUT delete is still refused (repo.Delete guard); authority links key off internal record.id not 001, so delete/renumber never breaks links. Already-pushed records from before this (e.g. 758) have no file until re-pushed; Delete tolerates the missing file. **Known gaps (DEFERRED):** Ctrl+S demote-to-draft leaves a stale file; changing a 001 then re-pushing orphans the old file; changing the output folder leaves old files behind. +6 mirror tests (238 green). **About Apud** dialog gained a **Legal** link → MIT License + "Copyright (c) Alonso Cossío Vázquez 2026".
- **Apud writes NOTHING into LDR/008 by itself except three approved exceptions (user CONFIRMED 2026-07-31: "those exceptions are exactly the right ones"):** (1) the Ctrl+F3 dialog pre-fills 008/00-05 with today's YYMMDD when that slot is blank (lands only on OK; Cancel writes nothing); (2) templates carry LDR/008 skeletons — but those are the USER'S portable .mrk files, not code; (3) Module 9 will derive 001/005/003 + leader record-length/base-address at push time. Everything else in the fixed fields is typed by hand. Frame the user re-derived himself and endorsed: **Apud is a text editor in philosophy** — it reads and writes one specific format, and catalographic soundness is the CATALOGUER's responsibility, not the software's. Keep it that way; any new automatic write needs his explicit approval.
- **Templates are already fully user-owned (clarified 2026-07-31 after confusion):** any .mrk in `templates\` beside the exe appears in the Ctrl+N picker; Ctrl+T saves the current record as one. This is his MOST PRACTICAL feature — he keeps a template per magazine subscription / book series / etc. **Template-picker subfolders and a filter box: OFFERED AND DECLINED (user, 2026-07-31) — flat alphabetical list is fine. Don't re-offer.**
- **Fixed-field layouts stay EMBEDDED in Marc.Core, not user-editable — OFFERED AND DECLINED (user, 2026-07-31):** "reasonable thing for not making the system smart, trusting the cataloguer; no need to change the layouts or make them editable." Do NOT externalize them to `layouts\` beside the exe. (They still govern the Ctrl+F3 dialog's labels/offsets and, later, Module 9's position validation.) Note this is a deliberate exception to design law #1 — he weighed it and chose embedded.
- **Displayed Year = 260/264 $c only, never the coded 008 date (user, 2026-07-31).** Real catalogues type brackets/fill/whole `[1960]` strings into the fixed 4-char 008 date slot, so the human transcription is the only date trustworthy in list/search columns. The 008 is still shown verbatim in the record view (and edited via Ctrl+F3) — this is display-source only, no data is rewritten. Fits the "faithful/dumb" ethos: don't paper over bad coded data, but derive display from the field a human actually wrote.
- **The Aleph LOOK is the point (user, 2026-07-28, Module 5.9):** Aleph is very dumb next to Koha and that's WHY we copy it — the record screen must feel like a FREE TEXT EDITOR, not boxes you fill out. 5.9 restyled the viewer (no grid lines, tight rows, red underlined tags/codes, bold data text on white — see docs/inspiration/README.md). **Module 6's editor must keep this: in-place editing on those text lines, never input boxes.** (The one place boxes are right: the Ctrl+F3 fixed-field dialog, Module 7.)
- **Editor dumbness (user, 2026-07-28):** new field = blank, any tag typed freely (judging tags is Module 9's job); editor never reorders fields — validation (Ctrl+L/W) does, as a stable sort by tag preserving the written order of repeated tags (650/500/900 order = cataloguing information). Old F5 pick-list and F11/F12 sort commands CUT. Full text in PLAN.md §6.2.
- **NO SMART BEHAVIOR (user, 2026-07-28, applies to ALL modules):** Aleph isn't smart and neither is Apud. Never reconnect to a previous catalogue (cataloguers open the wrong base out of habit — every session starts disconnected, choosing the database is a conscious act); never auto-create anything (no folders, no databases — SQLite's create-on-open is guarded off; File→New is the only creator, explicitly); no remembered state between sessions (AppSettings store deleted). Convenience features must be proposed and approved, never assumed. **APPROVED EXCEPTION (user, 2026-08-01, explicit): the last File-dialog folder IS remembered across sessions (`AppState`/`%APPDATA%\Apud\ui.json`, `LastFolder` only) — Open/New/Import/Export reopen there. Nothing else is remembered; catalogue/base still start disconnected every session.**
- **English-only for this whole version (user, 2026-07-28):** all UI text in English; Spanish/localization arrives in v2 or v3, well after first release. Module 10 loses its i18n item (settings/help remain).
- Name: **Apud**. Repo root `C:\Users\ACV\Projects\Apud\`; projects Marc.Core / Apud.Data / Apud.Sync / Apud.App / Apud.Tests.
- Single-machine philosophy; hub-and-spoke for teams; live multi-user REJECTED.
- **001 / accession numbers — SPECIALLY DUMB (user directive, 2026-07-31, for Module 9):** Ctrl+L auto-writing the next accession number is WANTED, but it must never conflict with his by-hand batch numbering (he hand-numbers batches, jumps between them, backfills old ones). Rules: (1) Ctrl+L fills a 001 ONLY when empty; a hand-typed 001 is kept forever, never overwritten. (2) The auto value = **live `MAX(existing 001 in that base) + 1`, computed at the moment of Ctrl+L — NOT a stored/persistent counter.** A stored counter is exactly what drifts out of step with his manual batches and would later collide; live max+1 always sits one past the top, so it can't collide, can't reuse, doesn't fill his gaps (he backfills by hand), and never renumbers anything. Per base (BIB/AUT separate). (3) A hand-typed 001 that duplicates an existing one in the base = plain validation error he resolves; Apud never silently renumbers. **He can change any 001 freely — nothing internal keys off it: authority links use the internal `record.id`, not the 001, so renumbering never breaks links or search.** Confirm at build time his 001s are plain integers (import showed next BIB=758); handle prefixed/non-numeric only if they exist. This SUPERSEDES the vaguer plan §8.5 "assigned from the sequence" — the `sequence` table + `BumpSequencePast` become vestigial (or a cache always reconciled to max+1); prefer dropping the counter for live max+1.
- Sync = SFTP backup/publish only (SSH.NET, atomic tmp+rename, VACUUM INTO).
- Auth links travel with fields in memory; fields+links rewritten in one transaction.
- Keymap is context-scoped config (keymap.json); menus render from command table.
- Working method: per-module loop = mini-spec list → build in order → user test-drives → user says "move on". Mid-build plan contradictions: stop, report, amend plan, continue. Discoveries not fixed now → docs/DEFERRED.md.
- STATE.md ritual: update before end-of-session summary, every session.
- **GitHub (plan change 2026-07-27, supersedes "Module 12" timing):** remote is `https://github.com/iratedemon04/apud.git`; push the whole repo at EVERY module close (and this initial wiring push). Repo created by user.
- **UI never displays dollar-sign subfield notation** (user decision): editor renders subfield codes as their own styled cells/boxes next to the text, Aleph-fashion; `$a` syntax lives only in .mrk files, never on screen. Applies from Module 6.
- **.mrk is the only file format** (import/export/templates); .mrc cut — MarcEdit handles conversion externally.
- Editor "spike" (throwaway UI prototype) proposed by Claude, REJECTED after discussion: user's option-value argument — core layers (Marc.Core/Apud.Data) are UI-independent, so a Module-6 UI surprise costs only thin-layer rework, not weeks; insurance not worth premium. Original 12-module sequence stands. Guiding frame: zero invention, 1960s solved problem, assembly not research; all durable logic lives below Apud.App.

## Next from user
- **Module 9 CODE-COMPLETE, awaiting his verdict.** Acceptance test-drive: open a draft, **Ctrl+W** → the findings list appears below the record; **click a row → the cursor jumps to that field**; fix an error; **Ctrl+L** → it pushes (fresh 001, fields reordered, `*` clears); then rename an AUT heading and Ctrl+L → linked bibs ripple with a count in the message bar. On "everything ok"/"move on" → Module 10 (handoff at the bottom). Watch for a veto on the 5 flagged scope calls (fixed-field warnings-only, unlinked-not-blocked, deferred lookup tables, per-record ripple, post-push warning behaviour). His `Ctrl+L = validate+push` assumption is now BUILT — if that binding is wrong he'll say.
- Working cadence agreed 2026-07-31: **one GO per module, not per step** (fresh session per module; STATE.md carries the handoff). Mid-build plan contradictions still stop-and-report.
- **His GO phrase is `apud module N, GO`** (that's literally how Module 7 started). It means: read STATE + PLAN, present the step list, and BUILD in the same turn — he does not want to approve the step list separately. Anything he'd veto he says after seeing it.
- Open items he has already DECLINED — do not re-offer: 006/007 layouts, user-editable layout files, template-picker subfolders/filter box. (See Decisions.)

## → HANDOFF TO NEXT INSTANCE: Module 10 (Settings + first-run wizard + F1 field help)
*(Next session, ONLY after he accepts Module 9. Read this file + docs/PLAN.md §10 (settings) + §6.2 (F1 = field help). **`apud module 10, GO` IS the go-ahead** — orient, present the step list, build in that same turn; he does not approve step lists separately. Flag scope calls; mid-build plan contradictions stop-and-report. **i18n is CUT from this version** (Decisions: English-only; Spanish → v2/v3) — Module 10 is Settings + first-run + F1 help only.)*
- **What it is (§10):** a Settings store + dialog and a first-run wizard so a new user on a clean machine reaches their first pushed record unaided, plus F1 field help. Settings to cover: institution **MARC org code (003 / 040$a)** — *this is the one Module 9 already reads via `repo.GetSetting("org_code")` to fill 003; wiring the setting turns 003 auto-fill on*; default language; default cataloguing conventions ($e rda); classification default (082/084); controlled-field set; data-folder location; keymap file; (server-sync settings belong to Module 11, stub or omit). **NO-SMART-BEHAVIOR still governs** (Decisions): the app never reconnects to a prior catalogue or remembers session state — settings are institution config, NOT remembered UI state. The `setting` table + `RecordRepository.GetSetting/SetSetting` already exist (used for org_code); Settings dialog is largely a UI over them.
- **F1 field help (§6.2):** offline MARC21 tag documentation panel for the field at the caret. TagNames.cs already maps tag→name; F1 shows fuller per-tag help. Scope: could be a simple shipped text/JSON per tag (config-contract like tagnames.json) — decide depth at step-list time (a one-paragraph-per-tag resource is plenty for v1).
- **Already built to lean on:** the config-contract pattern (keymap.json / tagnames.json / **profile-*.json** from M9) — missing→default, bad→report in the startup message line, never crash; copy it for any new config file. `MainForm` startup already aggregates `configReports` (add new loaders there). `F1` is a rebindable command id waiting in the keymap (secondary default, §6.2) — currently unbound/stub; wire `help.field` to it.
- **Reuse note for 003:** Module 9's auto-fill writes 003 ONLY when `org_code` is set and non-blank (invents nothing when unset — ethos). So the Settings org-code field is the switch that makes 003 appear on push; mention this in the acceptance step ("set your org code, push a record, watch 003 fill").
- **Gotcha:** the faithful/dumb ethos still holds — Settings add institution CONFIG (portable, per design law #1), not smart behaviour. Any new automatic write to a record still needs his explicit OK (the three approved exceptions are unchanged; 003 via org_code is exception #3, already approved).

## Standing working rules (fresh-session refresher)
- One module at a time; user gates transitions; step list before building.
- His live data (`TRABAJO DIARIO\BAC catalog\...`) is READ-ONLY, forever; he does his own real imports through the app.
- Every module close: update this file → commit → push to https://github.com/iratedemon04/apud.git (identity: repo-local config, iratedemon04).
- Test gate: `dotnet test` green before any commit; publish via publish.ps1 when there's something to test-drive.
- Discoveries not fixed now → docs/DEFERRED.md (one line each).
- UI never shows dollar-sign subfield notation; .mrk files are the only file format.
- §6.2 keymap red-pen — DONE (received 2026-07-28, folded into PLAN §6.2).
- GitHub repo creation — Module 12, I'll specify what I need then.
